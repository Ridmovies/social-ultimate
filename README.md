```
app/
│
├── main.py
│   # Точка входа.
│   # Создание FastAPI приложения, подключение api_router,
│   # middleware, обработчиков событий (startup/shutdown).
│
├── core/
│   ├── config.py
│   │   # Конфигурация приложения.
│   │   # Pydantic Settings, переменные окружения, URL БД, ключи, флаги.
│   │
│   ├── security.py
│   │   # Безопасность.
│   │   # Хэширование паролей, JWT (создание/проверка токенов),
│   │   # OAuth2 схемы.
│   │
│   ├── dependencies.py
│   │   # Глобальные зависимости FastAPI.
│   │   # Например: get_current_user, get_current_admin.
│   │
│   └── logging.py
│       # Настройка логирования.
│       # Формат логов, уровни, интеграции (stdout, файлы, внешние сервисы).
│
├── api/
│   ├── router.py
│   │   # Корневой роутер API.
│   │   # Подключает версии API (v1, v2).
│   │
│   └── v1/
│       ├── endpoints/
│       │   ├── auth.py
│       │   │   # HTTP-ручки авторизации.
│       │   │   # Логин, регистрация, refresh токенов.
│       │   │
│       │   ├── users.py
│       │   │   # CRUD пользователей.
│       │   │   # Только HTTP-слой: принимает запрос → вызывает сервис.
│       │   │
│       │   ├── posts.py
│       │   │   # Работа с постами (создание, лента, удаление).
│       │   │
│       │   ├── comments.py
│       │   │   # Комментарии к постам.
│       │   │
│       │   └── notifications.py
│       │       # Уведомления пользователя.
│       │
│       └── deps.py
│           # Зависимости уровня API v1.
│           # Например: пагинация, фильтры, специфичные проверки.
│
├── models/
│   ├── base.py
│   │   # Базовый класс ORM (SQLAlchemy Declarative Base).
│   │
│   ├── user.py
│   │   # ORM модель пользователя (таблица users).
│   │
│   ├── post.py
│   │   # ORM модель поста.
│   │
│   ├── comment.py
│   │   # ORM модель комментария.
│   │
│   └── notification.py
│       # ORM модель уведомления.
│
├── schemas/
│   ├── user.py
│   │   # Pydantic схемы пользователя.
│   │   # UserCreate, UserUpdate, UserResponse.
│   │
│   ├── post.py
│   │   # Схемы постов.
│   │
│   ├── comment.py
│   │   # Схемы комментариев.
│   │
│   └── notification.py
│       # Схемы уведомлений.
│
├── services/
│   ├── auth_service.py
│   │   # Бизнес-логика авторизации.
│   │   # Проверка пароля, генерация токенов.
│   │
│   ├── user_service.py
│   │   # Бизнес-логика пользователей.
│   │   # Регистрация, профиль, проверки прав.
│   │
│   ├── post_service.py
│   │   # Логика постов.
│   │   # Создание, лента, лайки (если есть).
│   │
│   ├── comment_service.py
│   │   # Логика комментариев.
│   │
│   └── notification_service.py
│       # Логика уведомлений.
│       # Создание, отправка, фильтрация.
│
├── repositories/
│   ├── base.py
│   │   # Базовый репозиторий.
│   │   # Общие CRUD-операции, работа с сессией.
│   │
│   ├── user_repo.py
│   │   # Доступ к данным пользователей.
│   │   # Запросы к таблице users.
│   │
│   ├── post_repo.py
│   │   # Запросы к постам.
│   │
│   ├── comment_repo.py
│   │   # Запросы к комментариям.
│   │
│   └── notification_repo.py
│       # Запросы к уведомлениям.
│
├── db/
│   ├── session.py
│   │   # Создание async engine и sessionmaker.
│   │   # Dependency get_db (yield AsyncSession).
│   │
│   ├── base.py
│   │   # Metadata для Alembic.
│   │   # Импорт всех моделей для автогенерации миграций.
│   │
│   └── migrations/
│       # Alembic миграции.
│       # Версионирование схемы БД.
│
├── utils/
│   ├── datetime.py
│   │   # Утилиты для работы с датами.
│   │
│   └── pagination.py
│       # Общая логика пагинации (limit/offset, курсоры).
│
└── tests/
    ├── conftest.py
    │   # Общие фикстуры pytest.
    │   # Тестовая БД, клиент, данные.
    │
    ├── test_users.py
    │   # Тесты пользователей.
    │
    └── test_posts.py
        # Тесты постов.
```